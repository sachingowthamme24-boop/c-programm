#include <stdio.h>

int main() {
    // Initialize an array of integers
    int numbers[5] = {10, 20, 30, 40,50};
int i;
    for (int i = 0; i < 5; i++) {
        printf("%d\n", numbers[i]);
    }

    return 0;
}